spinlock_tとは、
あるプロセスなどがロック(使用権を獲得するとcpuが割り込みなどをさせないために制御を行う)を獲得するまでずっと定期的にロックに空きがないかをチェックしながら待つ方式。
このスピンロックを実現するための構造体である。

list_headとは、
連結リストを実現するための構造体

struct list_head{
	struct list_head *prev;
	struct list_head *next;
};

というようにリストのヘッドの前のアドレスと次のアドレスを格納している。


wait_queue_head_t 型変数とは、
プロセスを待ち状態にするための待ち列と呼ばれるデータ構造

リストからheadをたどるには、
list_for_each()とlist_entry()の2つのマクロを使用する

list_for_each()は、

ーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーー
queueについて
fifo型でリストに追加した順番にリストがつながるタイプのリストを作ることができる。

list_add_tailを使うことにより、